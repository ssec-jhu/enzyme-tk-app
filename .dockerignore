# Version control
.git/
.gitignore

# CI / tooling
.github/
.tox/
githooks/

# Documentation & coverage reports
docs/
coverage.html/
coverage.xml

# Python build artifacts
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Dev / test files
tests/
tox.ini
Makefile
codecov.yml
CODEOWNERS
CODE_OF_CONDUCT.md
CONTRIBUTING.md
AGENTS.md

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store

# Docker (prevent recursive copy)
Dockerfile
docker-compose.yml
docker-compose.dev.yml
docker-compose.prod.yml
docker-compose.verify.yml

# Large external-tier data — MUST stay out of the image.  These dirs are
# mounted from the host (dev) or cloud storage (prod) at runtime via the
# ETK_DATA_DIR env var.  Only `enzyme_tk_app/app/data/structures/` is
# bundled into the image (kept allowed by omission below).
enzyme_tk_app/app/data/foldseek_db/
enzyme_tk_app/app/data/foldseek_models/
enzyme_tk_app/app/data/sequences/
enzyme_tk_app/app/data/reactions/

# Repo-root host bind-mount source — never bake into the image.
data/

# Env files
.env
.env.prod
.env.*.local